#install server side
install.packages("plumber")

#install client side to get the result
install.packages("httr")

#Require the package so you can use it
require("httr")

install.packages("jsonlite")

#Require the package so you can use it
require("jsonlite")

#no need, just to explore another package of RCurl : https://technistas.com/2012/06/11/using-rest-apis-from-r/
install.packages("RCurl","XML")